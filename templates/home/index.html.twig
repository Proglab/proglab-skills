{% extends 'base.html.twig' %}

{# Le nom de la page, et lui seul : le gabarit y ajoute « — <nom du dérivé> ». #}
{% block title %}{{ 'home.index.title'|trans }}{% endblock %}

{% block body %}
    <h1>Socle ERP</h1>

    {% if modules is not empty %}
        <ul data-modules>
            {% for module in modules %}
                <li>{{ module }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
