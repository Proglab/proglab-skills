# Lentille prose — EXPERIENCE.md (bmad-review, doc_standards)

Voix à préserver : registre technique, phrases courtes, cellules télégraphiques, titres de section en anglais canonique, chemins en chevrons (« Sidebar › Utilisateurs »), marques `[ASSUMPTION]`, puces à amorce en gras, maximes (« un bouton absent n'est pas une protection »), récit au présent dans les Key Flows avec chute narrative. Microcopy : rien à signaler.

| Pass | Texte d'origine | Texte révisé | Changements |
|---|---|---|---|
| prose | Tout le document — 342 signes doubles et 283 paires de « » en espace ordinaire | Espace fine insécable (U+202F) avant : ; ? ! et à l'intérieur des « », hors code | Passe regex après la structure |
| prose | « valeur à confirmer avec l'utilisateur » ; « confirmée par l'utilisateur » | « valeur à confirmer avec le pilote » ; « confirmée par le pilote » | Décision auteur : « pilote » est le terme du PRD pour Fabrice ; « utilisateur » est le rôle User |
| prose | Foundation › Pile : « Où vit chaque comportement est dit composant par composant dans Component Patterns, selon la seule question du skill : *où vit l'état ?* » | « Component Patterns dit, composant par composant, où vit chaque comportement, selon la seule question du skill : *où vit l'état ?* » | Voix active |
| prose | Surfaces › Sans interface : « **FR-17** a une interface pour la gestion des jetons (Profil), pas pour l'obtention par l'API elle-même. » | « **FR-17** a une interface pour gérer les jetons (Profil), pas pour les obtenir par l'API. » | Verbes |
| prose | « Spine-only (décision memlog) » | « Spine-only (décision du run UX, consignée dans `.memlog.md`) » | Décision auteur : jargon explicité |
| prose | Sheet : phrase de 70 mots « Clic sur une ligne ouvre le panneau… gardent leur état. » | « Un clic sur la ligne ouvre le panneau à droite. Le lien vit dans la première cellule et porte un nom complet : la date en texte visible, le reste en `sr-only` (« Ouvrir l'entrée… »). Un `::after` sur ce seul lien étend la zone cliquable à la ligne ; les autres cellules restent sélectionnables. Les filtres et la pagination restent derrière et gardent leur état. » — même traitement pour Table (« Ligne cliquable = lien dans la première cellule… ») | Scission |
| prose | Champ de formulaire : « Label réel toujours (règle 3). […] Validation serveur seule ; réponse 422 réaffiche… ; si l'erreur n'a pas de champ…, un bloc… » | « Un label réel, toujours (règle 3). […] La validation est serveur seule. Une réponse 422 réaffiche le formulaire avec les erreurs sous chaque champ (`aria-invalid`, `aria-describedby`), le focus sur le premier champ en erreur. Si l'erreur n'a pas de champ (message générique de Connexion), un bloc `role="alert"` `tabindex="-1"` au-dessus du formulaire la porte et reçoit le focus. » | Phrases complètes |
| prose | Champ de code 2FA : « lien vers une page distincte au champ identique » | « lien vers une page distincte qui porte le même champ » | Ambiguïté |
| prose | Dialog : « Le bouton confirmant est un `<form method="post">` avec jeton CSRF. » | « Le bouton de confirmation soumet un `<form method="post">` porteur d'un jeton CSRF. » | Sens |
| prose | Key Flows, 7 occurrences « clique « … » », « clique la ligne », « clique le lien » | « clique sur … » partout | Style Microsoft FR |
| prose | « tapable », « se tape en entier », « détail au tap » | « un appui sur la ligne ouvre le détail » ; « Une ligne de tableau répond à l'appui sur toute sa surface. » ; « détail d'un appui sur la ligne » | Décision auteur : « appui » |
| prose | « re-rend le message » ; « re-rend la page » | « réaffiche le message » ; « réaffiche la page » | Terminologie |
| prose | Accessibility Floor, chapeau Turbo : « En plus, pour le lecteur d'écran sous Turbo Drive — où une navigation ne recharge pas la page, si bien qu'aucun titre… : » | « Sous Turbo Drive, une navigation ne recharge pas la page : ni le titre, ni le `h1`, ni un message présent au rendu ne sont annoncés d'eux-mêmes au lecteur d'écran. Le socle y répond ainsi : » | Phrase-tiroir |
| prose | Focus (déplacé en Accessibility Floor) : « Le socle l'écrit : un contrôleur Stimulus `page-focus`… sinon… sinon… » | « Le socle le prend en charge lui-même. Un contrôleur Stimulus `page-focus` posé sur `<main>` — son `connect()` s'exécute à chaque remplacement de body, pas seulement au premier chargement (`symfony-proglab-frontend`) — place le focus sur le premier élément présent parmi : 1. le bloc d'erreur générique `role="alert"` ; 2. le premier champ `aria-invalid` ; 3. le Flash (`tabindex="-1"`) ; 4. le `h1` (`tabindex="-1"`). » | Liste ordonnée |
| prose | Annonces : « …copie son texte dans `#annonces` à `connect()` (…), `aria-live="assertive"` pour une erreur. » | « …copie son texte dans `#annonces` à `connect()` (un instant après l'insertion, pour que le changement soit perçu) ; pour une erreur, la région passe en `aria-live="assertive"`. » | Décision auteur : c'est la région qui passe en assertive |
| prose | Titre : « (par exemple « Utilisateurs — Menuiserie Dubois ») (WCAG 2.4.2) » | « (par exemple « Utilisateurs — Menuiserie Dubois » ; WCAG 2.4.2) » | Parenthèses |
| prose | DropdownMenu : « Un choix soumet un POST et la page se recharge dans la nouvelle langue / le nouveau thème. Thème : […] » | « Un choix soumet un POST ; la page revient dans la nouvelle langue ou le nouveau thème. Pour le thème, Stimulus applique le choix côté client sans attendre la réponse ; […] » | Décision auteur : reformulation, règle inchangée |
| prose | Chargement de page : « (feedback fonctionnel, > 500 ms) » | « (retour fonctionnel, visible seulement au-delà de 500 ms) » | Décision auteur : seuil d'apparition |
| prose | « la quasi-totalité des utilisateurs y sont » | « la quasi-totalité des utilisateurs y est » | Accord |
| prose | Key Flows, paragraphes « Cas limite » en chaînes de flèches (UJ-1, UJ-2, UJ-5, Flux 7, Flux 8) | Réécrire en phrases ; « → » réservé aux cellules de tableau. Ex. UJ-2 : « Karim n'a pas cliqué dans les 7 jours. Sur sa Fiche, l'État passe à « Invitation expirée » et le bouton « Renvoyer l'invitation » ouvre un Dialog court : « … ». À la confirmation, Flash « Invitation renvoyée. » ; l'ancien lien affiche l'état « Invitation expirée ». » | Décision auteur : accepté |
| prose | Mineurs : « au 8e jour » → « le huitième jour » ; « contenu d'une story » → « contenu d'une tâche (story BMAD) » ; préambule « citées par leur nom » → « que les deux documents citent par leur nom » ; « c'est ce qui fait lire le nouveau contexte » → « c'est ce déplacement qui fait lire le nouveau contexte au lecteur d'écran » ; « Trop de chrome pour Marc » → « trop d'habillage » ; « Bouton de soumission désactivé pendant l'envoi (Turbo le fait) » gardé une seule fois (Champ de formulaire), renvoi ailleurs | appliquer | |

Bilan : 20 lignes (13 fermes, 7 « Consider » tranchées ci-dessus), ≈ +40 mots ; aucune règle, chiffre, chaîne d'interface ni `{token}` modifié.
