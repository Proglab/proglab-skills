<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnPhpunitNotice="true"
         displayDetailsOnPhpunitNotices="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
         defaultTestSuite="Project Test Suite"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <!-- Deux testsuites, et un défaut.

         « Accessibility » est la sixième catégorie de la porte de qualité : elle a sa
         propre cible `make a11y` et son propre job de CI, pour qu'un échec dise « le
         plancher d'accessibilité est cassé » plutôt que « un test a échoué quelque part ».
         Sans `defaultTestSuite`, `php vendor/bin/phpunit` sans option lancerait les deux
         suites, et la catégorie « Tests » rejouerait le plancher — deux catégories rouges
         pour un seul défaut, et le double du temps sur chaque exécution locale. -->
    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
            <exclude>tests/Core/Accessibility</exclude>
        </testsuite>

        <testsuite name="Accessibility">
            <directory>tests/Core/Accessibility</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <!-- Isolation de la base de données. Cette ligne seule ne suffit pas : le bundle
         doit aussi être dans config/bundles.php sous 'test', sinon l'extension
         s'exécute, ne trouve aucune connexion statique à envelopper, et chaque test
         valide (commit) — la suite reste verte pendant qu'elle remplit la base. -->
    <extensions>
        <bootstrap class="DAMA\DoctrineTestBundle\PHPUnit\PHPUnitExtension"/>
    </extensions>

</phpunit>
